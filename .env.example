# NextAuth (Authentication)
# Used for session encryption. You can generate a strong secret by running:
# `openssl rand -hex 32`
NEXTAUTH_SECRET=

# Google OAuth credentials (get from Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database (PostgreSQL)
# Connection string for your Postgres database (e.g., from Neon or Vercel).
# Example: postgres://user:password@host:port/dbname?sslmode=require
DATABASE_URL=

ADMIN_EMAILS = 